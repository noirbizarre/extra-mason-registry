---
name: 🚀 Release

on:
  push:
    branches:
      - main

jobs:
  release:
    name: 📦 Release registry
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: mason-org/actions/registry-release@v1
